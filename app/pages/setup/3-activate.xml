<!--
Here the user can enter his alias
-->
<Page xmlns="http://schemas.nativescript.org/tns.xsd" navigatingTo="navigatingTo" class="page">

    <StackLayout class="p-20">
        <Label text="Activate your identity" class="h1 text-center"
               textWrap="true"/>
        <Label text="Get this QRCode scanned by somebody who has a personhood-badge"
               class="h3"
               textWrap="true"/>
        <Image src="{{ qrcode }}"
               visibility="visible"
               width="30%"/>
        <Label text="Or chose another activation method:" class="h3 text-center"
               textWrap="true"/>
        <!--<Button text="Receive personhood-coins" tap="activatePersonhood" class="btn btn-primary btn-stub"/>-->
        <Button text="Visit a personhood-party" tap="activateParty" class="btn btn-primary btn-stub"/>
        <Button text="Login through EPFL" tap="activateEPFL" class="btn btn-primary btn-stub"/>
        <Button text="Use email" tap="activateEmail" class="btn btn-primary btn-stub"/>
        <Button text="Pay for account" tap="activateEmail" class="btn btn-primary btn-stub"/>
        <Button text="Delete all" tap="deleteAll" class="btn btn-primary btn-stub"/>
    </StackLayout>
</Page>
