<Page xmlns="http://schemas.nativescript.org/tns.xsd"
      xmlns:df="nativescript-ui-dataform"
      navigatingTo="navigatingTo"
      class="page">
    <ActionBar class="action-bar">
        <Label class="action-bar-title" text="Settings"></Label>
    </ActionBar>
    <StackLayout class="p-20">
        <Label text="Admin" class="h1 text-center"/>
        <df:RadDataForm id="myDataForm" source="{{ admin }}" />
        <Button text="Clear all data" tap="tapClear" class="btn btn-test"/>
        <Button text="Create Party" tap="tapCreateParty" class="btn btn-stub"/>
        <Button text="Save" tap="tapSave" class="btn btn-primary btn-active"/>
    </StackLayout>
</Page>
